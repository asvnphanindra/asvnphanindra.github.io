---
import BaseLayout from '../layouts/BaseLayout.astro';
import { featuredProducts } from '../data/products';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog', ({ data }) => !data.draft)).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
const latest = posts.slice(0, 3);
---

<BaseLayout title="ASVN Phanindra — Home">
  <section class="hero reveal">
    <p class="hero-brand">ASVN Phanindra</p>
    <h1 class="hero-title">Building AI products, teaching tools, and O-RAN research.</h1>
    <p class="hero-lede">
      Selected courses, simulators, and classroom software — plus short notes from the work.
    </p>
    <div class="hero-actions">
      <a class="btn btn-primary" href="/products">View products</a>
      <a class="btn btn-ghost" href="/blog">Read the blog</a>
    </div>
  </section>

  <section class="section reveal" style="--reveal-delay: 80ms">
    <div class="section-head">
      <h2>Featured products</h2>
      <a class="text-link" href="/products">All products</a>
    </div>
    <ul class="product-grid">
      {
        featuredProducts.map((product) => (
          <li>
            <a
              class="product-card"
              href={product.href}
              target="_blank"
              rel="noopener noreferrer"
            >
              <span class="product-card-name">{product.name}</span>
              <span class="product-card-copy">{product.oneLiner}</span>
              <span class="product-card-cta">Open</span>
            </a>
          </li>
        ))
      }
    </ul>
  </section>

  <section class="section reveal" style="--reveal-delay: 160ms">
    <div class="section-head">
      <h2>Latest posts</h2>
      <a class="text-link" href="/blog">Blog</a>
    </div>
    {
      latest.length === 0 ? (
        <p class="empty">No posts yet.</p>
      ) : (
        <ul class="post-list">
          {latest.map((post) => (
            <li>
              <a class="post-row" href={`/blog/${post.slug}/`}>
                <span class="post-row-title">{post.data.title}</span>
                <time datetime={post.data.pubDate.toISOString()}>
                  {post.data.pubDate.toLocaleDateString('en-US', {
                    year: 'numeric',
                    month: 'short',
                    day: 'numeric',
                  })}
                </time>
              </a>
            </li>
          ))}
        </ul>
      )
    }
  </section>
</BaseLayout>
